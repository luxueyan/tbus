{
    "name": "theh-mobile",
    "os": ["!win32"],
    "scripts": {
        "start": "cd dist/ && ecstatic",
        "build": "./build.sh",
        "test": "echo 'no test yet' && exit 0",
        "reload": "echo 'served as static site so nothing to do here.'"
    },
    "dependencies": {
        "ecstatic": "^0.7.4"
    }
}
